name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  package-tests:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift package tests (176+ BIP vector + unit + loopback)
        run: swift test
        # Env-gated suites (DifferentialTests, signet integration) skip
        # cleanly without BTC_SWIFT_DIFF/BTC_SWIFT_SIGNET and no local node.

  app-build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate Xcode project
        run: brew install xcodegen && xcodegen
      - name: Build app (iOS Simulator)
        run: >-
          xcodebuild -project BTCSwiftApp.xcodeproj -scheme BTCSwiftApp
          -destination 'generic/platform=iOS Simulator'
          -skipPackagePluginValidation -skipMacroValidation build
